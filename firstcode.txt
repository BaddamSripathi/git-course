hello world !!!
i have to learn springboot,react
git diff //gets differencees in two versions(unstaged stage)
git diff --staged //differences in staged
git rm --cached <file_name> //removing file from git
//working directory,stagging,adding to git,remote reporisitory
git clone ssh key/https key //getting project from remote reporisitory
ssh-keygen -o 
cat <file_name>//gives content of that file
git remote add origin <sshkey>
git push -u orgin main
git tag //gives all tags
git tag -a v1.0 -m "1st release"//tagging two types annotated tagging,lightweight tagging
git switch -c <branch_name>
git branch -d <branch_name>
git merge <branch_name>//go to main before merging

